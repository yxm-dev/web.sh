[web.sh](https://github.com/yxm-dev/web.sh) is a Bash function focused on people that like to work in a
terminal emulator but also need to use a browser. It makes easy to search in browsers or open bookmarked pages
from terminal commands.
