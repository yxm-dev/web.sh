`web.sh` is a bash function focused on people that like to work inside a terminal emulator but also need  to
use a browser. It makes easy to search in browsers or open bookmarked pages from terminal commands.
